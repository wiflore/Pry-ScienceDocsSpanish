# Backend al que apunta el dev server (a través del proxy /api de Vite)
# El proxy strippa origin, así que no hay tema CORS aún si el backend no lo permite
VITE_API_BASE_URL=http://100.27.211.193:8000

# Path que usa el cliente HTTP — el proxy de Vite lo convierte a la URL real
VITE_API_PATH=/api
